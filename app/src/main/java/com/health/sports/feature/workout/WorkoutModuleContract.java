package com.health.sports.feature.workout;

/**
 * Module B entry point placeholder.
 *
 * Suggested scope: outdoor workout session, GPS track points, workout summary,
 * and local cache before backend upload.
 */
public interface WorkoutModuleContract {
    String MODULE_NAME = "运动记录模块";
}
